.col-10
  %h5= "Lista de capítulos privados a los que tienes accesso:"
  .border.border-opacity-10.rounded-3.p-4.mt-3
    %table.table.table-hover.mb-0
      %thead
        %tr
          %th.border-top-0{:scope => "col"} #
          %th.border-top-0{:scope => "col"} Título
          %th.border-top-0{:scope => "col"} Donation (usd)
          %th.border-top-0{:scope => "col"} Orden ID
          %th.border-top-0{:scope => "col"} Ver
      %tbody
        - current_user.paid_orders.each_with_index do |order, index|
          %tr
            %th{:scope => "row"}= index
            %td.border-bottom-0= order.post.title
            %td.border-bottom-0= order.price
            %td.border-bottom-0= order.charge_id
            %td.border-bottom-0= link_to "Ver", post_path(order.post.friendly_id), class: ""

  %hr.my-5/